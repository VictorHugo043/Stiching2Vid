# 11_decision_log

## 使用规则
- append-only，不回写历史结论。
- 每条记录都必须带唯一 ID。
- 如后续推翻旧决策，新增一条新决策并引用旧 ID，而不是修改旧条目。

## 决策记录
| ID | 日期 | 决策 | 原因 | 影响 | 替代方案 | 关联文件 |
| --- | --- | --- | --- | --- | --- | --- |
| DEC-20260319-01 | 2026-03-19 | 沿用现有 `ai-docs/current/00~07` 编号体系，不新建平行文档树 | 现有结构已经稳定，且按阶段编号组织清晰 | 后续所有新文档采用 `08+` 编号追加 | 新建平行 wiki 式目录，未采用 | `ai-docs/current/*` |
| DEC-20260319-02 | 2026-03-19 | 总体优先级固定为 `Phase 0 -> Phase 1 -> Phase 2 -> Phase 3 -> Phase 4` | Method B、Dynamic seam、evaluation 是核心研究任务，GUI 是包装层 | 后续排期必须先完成基线冻结与 Method B 设计，再做 GUI | 先做 GUI，未采用 | `08_project_status_and_master_plan` |
| DEC-20260319-03 | 2026-03-19 | Method B 默认采用预训练 `SuperPoint + LightGlue + OpenCV USAC_MAGSAC` | 当前项目更适合接入预训练与现成 robust estimator，而非训练新模型 | Method B 设计将围绕 optional dependency、lazy import、fallback 展开 | 训练新模型；引入独立 MAGSAC++ 实现，未采用 | `06_method2_strong_matching` |
| DEC-20260319-04 | 2026-03-19 | Dynamic seam 采用“两层路线”：先兼容 OpenCV seam finder 的 MVP，再做 advanced 新 seam backend | 当前 seam 模块是 mask 风格，直接复现 object-centered MRF 风险过高 | Phase 2 优先做 `keyframe/trigger seam policy + object-aware penalty + seam smoothing` | 一步到位完整复现 ECCV 2018 MRF，未采用 | `09_dynamic_seam_and_temporal_eval` |
| DEC-20260319-05 | 2026-03-19 | 运行模式文档层显式拆分为 `fixed_geometry / keyframe_update / adaptive_update` | 当前 `reuse_mode` 名称与真实行为不完全一致，且 `jitter` 解释依赖模式 | Phase 0 必须先修正文档与评测语义 | 继续沿用旧术语且不解释差异，未采用 | `03_baseline_video_pipeline`, `05_evaluation`, `09_dynamic_seam_and_temporal_eval` |
| DEC-20260319-06 | 2026-03-19 | GUI 只做 thin wrapper，不重写核心 stitching pipeline | 当前核心 pipeline 与 artefacts 已可复用，过早侵入核心会增加返工 | GUI 只复用 CLI、I/O 和 outputs bundle | 在 GUI 中重写运行逻辑，未采用 | `08_project_status_and_master_plan` |
| DEC-20260319-07 | 2026-03-19 | Phase 0 先保留 `run_baseline_video.py` 的 legacy CLI 参数，但在导出 artefacts 中显式冻结 `geometry_mode` 和 `jitter_meaningful` | 当前首要目标是稳定语义与评测解释，而不是提前破坏 CLI 兼容性 | `transforms.csv`、`metrics_preview.json`、`debug.json` 以派生字段承载模式语义；当前仅导出 `fixed_geometry` 或 `keyframe_update`，`adaptive_update` 保留给后续实现 | 立即重命名 CLI 参数并切断旧字段，未采用 | `scripts/run_baseline_video.py`, `03_baseline_video_pipeline`, `05_evaluation` |
| DEC-20260319-08 | 2026-03-19 | `scripts/ablate_temporal.py` 与 `scripts/ablate_seam.py` 保留为 legacy exploratory helpers，不再作为当前 Phase 0 / Phase 1 的正式入口或验收依赖 | 这两个脚本只是早期便捷封装，继续把它们当成 schema 冻结的一部分会误导排期 | Phase 0 可在不改这两个脚本消费逻辑的前提下闭环；未来若需要正式实验驱动，另建统一 experiment driver | 立即删除两个脚本；继续把它们当主线入口，均未采用 | `05_evaluation`, `08_project_status_and_master_plan`, `scripts/ablate_temporal.py`, `scripts/ablate_seam.py` |
| DEC-20260319-09 | 2026-03-19 | Phase 1 的接口改造采用“新增结果对象接口、保留 legacy tuple/OpenCV 接口”的兼容策略 | 需要先让单帧路径切到统一数据边界，同时避免本步牵动 `run_baseline_video.py` | `run_baseline_frame.py` 先改用 `FeatureResult / MatchResult / GeometryResult`；视频路径后续再迁移或加 adapter | 直接改旧函数返回类型；直接同步重构视频路径，均未采用 | `src/stitching/features.py`, `src/stitching/matching.py`, `src/stitching/geometry.py`, `scripts/run_baseline_frame.py`, `06_method2_strong_matching` |
| DEC-20260319-10 | 2026-03-19 | 当前 `run_baseline_frame.py` 明确定位为单帧 feature/matching/geometry/backend smoke 入口，而不是视频质量链路的视觉对齐参考 | 视频入口已叠加 seam、crop、cache 与 temporal 逻辑，单帧入口尚未同步这些能力 | 后续讨论单帧 smoke 结果时不得把它直接与视频最终 stitched 质量做一一对应；检查 seam/crop 必须以 `run_baseline_video.py` 为准 | 立即给单帧入口补齐 seam/crop；继续默认它与视频质量链路等价，均未采用 | `02_baseline_frame_stitch`, `03_baseline_video_pipeline`, `06_method2_strong_matching`, `scripts/run_baseline_frame.py`, `scripts/run_baseline_video.py` |
| DEC-20260319-11 | 2026-03-19 | 可以把“单帧尽量贴近视频质量链路的 seam/crop 预览入口”纳入 Phase 1，但只能作为支持子任务，且应排在 Method B 单帧 backend loader 之后 | 这个能力对 Method B 的 qualitative 检查有价值，但不属于 Method B 主线算法接入本身；过早实现或直接复制视频脚本都会造成 scope creep | Phase 1 可新增 `frame_quality_preview` 子任务；实现上优先抽共享 helper / adapter，而不是复制 `run_baseline_video.py` 的内联质量链路 | 完全推迟到 Phase 2；或立即在当前子任务中直接补齐单帧 seam/crop，均未采用 | `02_baseline_frame_stitch`, `06_method2_strong_matching`, `08_project_status_and_master_plan`, `scripts/run_baseline_frame.py`, `scripts/run_baseline_video.py`, `src/stitching/video_stitcher.py` |
| DEC-20260319-12 | 2026-03-19 | `frame_quality_preview` 的最小实现采用“新增共享 helper 包装 `VideoStitcher.initialize_from_first_frame()`”的方案，不改 `run_baseline_video.py` 主逻辑 | 这样可以直接复用现有 `crop -> seam -> blend` 质量链路，且避免复制视频脚本中的大段内联 compose 逻辑 | 单帧入口现在能产出更接近视频静态观感的 seam/crop/blend 结果；视频入口后续仍可独立演进 | 直接把视频脚本的大段 seam/crop 逻辑复制到 `run_baseline_frame.py`；或继续保留 feather-only 单帧输出，均未采用 | `src/stitching/frame_quality_preview.py`, `scripts/run_baseline_frame.py`, `src/stitching/video_stitcher.py`, `02_baseline_frame_stitch`, `06_method2_strong_matching` |
| DEC-20260319-13 | 2026-03-19 | 单帧 Method B loader 采用“真实 optional backend + lazy import + structured diagnostics + optional fallback”的方案，即使当前环境缺依赖也先把失败语义和配置面固定下来 | 当前最大风险不是算法逻辑，而是依赖、weights、device 与失败行为不透明；先把 loader 和 diagnostics 固定，后续只需补环境即可验证成功路径 | `superpoint / lightglue` 不再只是静态占位；`run_baseline_frame.py` 可记录 requested/effective backend、fallback events 和 dependency diagnostics；视频路径暂不受影响 | 继续保留纯 `NotImplementedError` 占位；或直接等待装好依赖后再实现，均未采用 | `src/stitching/method_b_runtime.py`, `src/stitching/features.py`, `src/stitching/matching.py`, `scripts/run_baseline_frame.py`, `06_method2_strong_matching` |
| DEC-20260320-01 | 2026-03-20 | LightGlue 输出解析层同时兼容官方 compact list 格式和 tensor 格式，不切换到官方 `utils.match_pair()` 封装 | 官方 `LightGlue.forward()` 返回的 `matches / scores` 在常规路径下是 batch-wise list；当前最小修复只需兼容输出格式，无需重做现有特征/匹配边界 | 单帧 Method B 成功路径可以直接复用当前 `FeatureResult / MatchResult` 设计；后续视频 adapter 也能沿用这层兼容 | 改用 `lightglue.utils.match_pair()`；或仅支持一种输出格式，均未采用 | `src/stitching/matching.py`, `external/LightGlue/lightglue/lightglue.py`, `06_method2_strong_matching` |
| DEC-20260320-02 | 2026-03-20 | 正式环境入口固定为 root `requirements*.txt` + `docs/environment.md`，现有本地 `.venv` / `.venv-methodb` 只作为可能漂移的实例，不再作为文档真值来源 | 当前机器上 system Python、`.venv`、`.venv-methodb` 的依赖状态并不一致，若直接把本地状态写成文档会继续制造漂移 | 后续环境复现、安装和使用说明统一以 root requirements 和环境文档为准；排查环境问题时优先重建或重新安装 | 继续只在 README 中保留口头安装说明；或把当前虚拟环境内容直接 freeze 为唯一真值，均未采用 | `requirements.txt`, `requirements-methodb.txt`, `docs/environment.md`, `README.md`, `14_open_issues_and_next_steps` |
| DEC-20260320-03 | 2026-03-20 | 多 pair 单帧回归采用独立 smoke suite wrapper 调用 `run_baseline_frame.py`，而不是引入 pytest 或复制单帧 stitching 逻辑 | 当前目标是把已有 CLI 单帧入口变成可重复执行的回归工具，并保留现有 bundle 产物；引入测试框架或复制逻辑都会扩大范围 | `scripts/run_frame_smoke_suite.py` 成为当前 frame regression 入口；Method A / Method B 均可复用该套件，默认覆盖 4 个代表性 pair | 直接用 shell 脚本串命令；引入 pytest；在 suite 脚本里重写 stitching 逻辑，均未采用 | `scripts/run_frame_smoke_suite.py`, `scripts/run_baseline_frame.py`, `docs/environment.md`, `06_method2_strong_matching` |
| DEC-20260320-04 | 2026-03-20 | 视频路径 adapter 采用“新增共享 `frame_pair_pipeline` helper，把 `run_baseline_video.py` 接到结果对象层，同时保持 `VideoStitcher` 只消费 `H / T / canvas_size`”的方案 | 直接在视频脚本内复制 Method B loader/fallback 逻辑会继续放大单体脚本；而重写 `VideoStitcher` 的几何职责会超出最小改动范围 | Method A / Method B 现在在视频入口共享同一套 frame-pair estimation 数据边界；seam/crop/blend/cache 语义保持不变 | 直接在 `run_baseline_video.py` 里复制单帧逻辑；或让 `VideoStitcher` 直接接管特征/匹配/几何估计，均未采用 | `src/stitching/frame_pair_pipeline.py`, `scripts/run_baseline_video.py`, `src/stitching/video_stitcher.py`, `06_method2_strong_matching`, `03_baseline_video_pipeline` |
| DEC-20260320-05 | 2026-03-20 | 在未额外澄清时，把本次“`mine_source pujiang`”的视频回归默认解释为 `mine_source_pujiang1_left_right`，并把该假设显式写入实施日志与复现命令 | `pairs.yaml` 中存在 `pujiang1 / pujiang2 / pujiang3` 三条；本步目标是先完成 Phase 1 的最小视频级回归，而不是阻塞在命名歧义上 | 当前 3 条 Method B 视频回归已可稳定复现；后续若用户需要 `pujiang2/3`，只需替换 pair id 重跑同一组命令 | 等待用户先明确 `pujiang1/2/3` 再跑；或同时把 3 条都跑一遍，均未采用 | `data/manifests/pairs.yaml`, `12_implementation_log`, `06_method2_strong_matching`, `08_project_status_and_master_plan` |
| DEC-20260320-06 | 2026-03-20 | Phase 1 的正式视频级 compare preset 固定为 `scripts/run_video_compare_suite.py`，并保留三条方法预设：`method_a_orb / method_a_sift / method_b` | 当前正式 compare 的目标既是验证 Method B 已完整接到视频质量链路，也是确认 Method A 内部 ORB/SIFT 与 Method B 的相对表现；提前删掉 ORB 或 SIFT 会丢失有效比较基线 | Phase 1 收尾以统一 compare driver、`summary.csv` 和 `pair_compare.csv` 为准；后续 Phase 3 继续沿用这三条预设做方法主轴 | 只保留单一 Method A 预设；继续手工拼命令和手抄表，均未采用 | `scripts/run_video_compare_suite.py`, `05_evaluation`, `06_method2_strong_matching`, `08_project_status_and_master_plan`, `README.md` |
| DEC-20260320-07 | 2026-03-20 | Phase 2 第一批只实现 seam policy 外壳和 meaningful temporal evaluation 的最小恢复，不重写 OpenCV seam backend | 当前最需要先固定的是“何时更新 seam”和“fixed geometry 下如何解释 temporal 指标”；若直接进入 object-aware / graph-cut，会提前扩大范围并破坏 Method A / Method B 共用路径 | `run_baseline_video.py`、`VideoStitcher` 和 bundle 字段先稳定支持 `fixed/keyframe/trigger` seam policy 与 `temporal_primary_metric`；后续再在此基础上补 object-aware penalty 和 seam smoothing | 直接做新 seam backend；或继续只依赖退化的 `jitter` 解释 fixed geometry，均未采用 | `src/stitching/seam_policy.py`, `src/stitching/video_stitcher.py`, `src/stitching/temporal.py`, `scripts/run_baseline_video.py`, `09_dynamic_seam_and_temporal_eval`, `05_evaluation` |
| DEC-20260320-08 | 2026-03-20 | `geometry_mode` 升级为正式用户入口，`video_mode` 保留为 legacy 兼容别名；`keyframe_every` 与 `seam_keyframe_every` 明确拆分为几何与 seam 两套 cadence | 继续让用户直接操作 `video_mode` 会把“几何路径”和“seam 路径”混在一起；而 Phase 2 之后的 trigger、object-aware penalty、GUI 参数都需要正交的配置面 | 新 run 应优先使用 `--geometry_mode`；`video_mode` 仅供旧脚本兼容；`geometry_mode` 决定几何更新路径，`seam_policy` 决定 seam 更新路径，`keyframe_every` 与 `seam_keyframe_every` 分属两层 | 继续只靠文档解释而不改 CLI；或直接删除 `video_mode` 破坏兼容，均未采用 | `scripts/run_baseline_video.py`, `03_baseline_video_pipeline`, `05_evaluation`, `08_project_status_and_master_plan`, `09_dynamic_seam_and_temporal_eval` |
| DEC-20260320-09 | 2026-03-20 | `fixed_geometry` 下的 `jitter` 明确只解释 geometry，不解释 seam；seam 重算事件必须通过独立 snapshot 通道留痕 | 用户已经在真实 run 中看到 `keyframe/trigger` seam 更新但 `jitter=0` 的情况，仅靠 `jitter_meaningful=0` 不够直观，且原先 `snapshot_every` 可能错过 seam 重算帧 | 输出层新增 `jitter_scope`，并引入 `--seam_snapshot_on_recompute`；后续调试 dynamic seam 时优先结合 `seam_recompute_count / seam_snapshot_count / overlap_diff_after` 判断 seam 是否生效 | 继续只靠文档解释；或把 seam 变化硬塞进 `jitter`，均未采用 | `scripts/run_baseline_video.py`, `src/stitching/video_stitcher.py`, `03_baseline_video_pipeline`, `05_evaluation`, `09_dynamic_seam_and_temporal_eval` |
| DEC-20260320-10 | 2026-03-20 | 不新增额外的 `geometry_update_on_seam_recompute` 布尔开关，而是直接把 `geometry_mode=adaptive_update` 的 MVP 落成 seam-driven geometry refresh | 用户希望“seam 变化时几何也可动态变化”；若再新增一个布尔开关，会把已建立的 `geometry_mode / seam_policy` 语义重新打散 | `adaptive_update` 现在表示：仍走 cached reuse，但当 `seam_policy=keyframe/trigger` 命中 seam event 时，在同一帧重估 geometry 并 re-init compose；bundle 同步导出 `geometry_update_count / geometry_update_events / geometry_recomputed / geometry_update_reason` | 新增独立开关 `geometry_update_on_seam_recompute`；或继续把 `adaptive_update` 保留为未实现占位，均未采用 | `scripts/run_baseline_video.py`, `03_baseline_video_pipeline`, `05_evaluation`, `08_project_status_and_master_plan`, `09_dynamic_seam_and_temporal_eval` |
| DEC-20260323-01 | 2026-03-23 | 当前 Phase 2 的 object-aware MVP 采用 `foreground_mode=disagreement` 和 protect-mask reassignment，而不引入 detector 或重写 seam energy | `mine_source_*` pairs 没有现成 object masks；当前更需要先验证兼容式 foreground-aware 外壳是否能改善 seam 行为 | `src/stitching/foreground.py` 成为当前 foreground/object-aware MVP 落点；后续可在保留该兼容层的前提下，再扩展到真实 object masks 或新 seam backend | 直接接 detector/segmentation；直接重写 graph-cut/object-centered seam backend，均未采用 | `src/stitching/foreground.py`, `src/stitching/video_stitcher.py`, `scripts/run_baseline_video.py`, `09_dynamic_seam_and_temporal_eval` |
| DEC-20260323-02 | 2026-03-23 | 当前 Phase 2 默认 seam preset 推荐 `geometry_mode=fixed_geometry + seam_policy=trigger + seam_trigger_diff_threshold=18 + foreground_mode=disagreement + seam_trigger_foreground_ratio=0.08`；`adaptive_fused` 仅保留为实验 preset | `phase2_trigger_adaptive_minesource_calib_v2` 表明该 preset 在 `mine_source_*` 上能明显降低 `mean_overlap_diff_after`，同时比当前 `adaptive_fused` 更快、更稳 | 后续 Phase 2 默认 qualitative / quantitative seam 对比优先使用 `trigger_fused_d18_fg008`；`adaptive_fused` 只在专门研究 geometry refresh 时启用 | 默认采用 `adaptive_fused`；或默认采用带全局 cooldown/hysteresis 的 stable preset，均未采用 | `scripts/run_phase2_trigger_calibration.py`, `05_evaluation`, `08_project_status_and_master_plan`, `09_dynamic_seam_and_temporal_eval` |
| DEC-20260323-03 | 2026-03-23 | `trigger_armed / hysteresis` 从单一全局状态细化为 per-trigger state（`overlap / diff / foreground`），cooldown 只对本次实际触发的通道生效 | sustained foreground 下，全局 armed 容易让 trigger controller 退化为“一次触发后长期不上膛”，从而把 `adaptive_update` 错误压成近似一次性事件 | `adaptive_update` 现在能在更长视频里产生多次 geometry refresh；调试时应同时查看 `trigger_reason / trigger_states_next / geometry_update_count` | 继续沿用单一全局 armed；或仅给 foreground trigger 单独打补丁，均未采用 | `src/stitching/seam_policy.py`, `src/stitching/video_stitcher.py`, `scripts/run_baseline_video.py`, `09_dynamic_seam_and_temporal_eval` |
| DEC-20260323-04 | 2026-03-23 | seam temporal smoothing 作为 OpenCV seam backend 外层的可选 mask-level smoothing 保留，但当前默认值仍是 `seam_smooth=none`；其评估优先看 `mean_seam_mask_change_ratio / mean_stitched_delta / approx_fps`，不把 `mean_overlap_diff_after` 作为主比较项 | full-length smoothing suite 表明 `ema/window` 能显著压低 seam mask 变化，但对 `mean_stitched_delta` 没有带来稳定收益；同时当前 `mean_overlap_diff_after` 在严格分区后的 smoothed mask 上会出现 0.0 的解释偏差 | Phase 2 当前不默认开启 smoothing；若要研究 flicker，可继续保留 `ema/window` 作为实验 preset，但文档和表格必须明确指标解释边界 | 默认开启 `ema`；或继续把 `mean_overlap_diff_after` 当 smoothing 主指标，均未采用 | `src/stitching/temporal.py`, `src/stitching/video_stitcher.py`, `scripts/run_baseline_video.py`, `scripts/run_phase2_seam_smoothing_suite.py`, `05_evaluation`, `09_dynamic_seam_and_temporal_eval` |
| DEC-20260323-05 | 2026-03-23 | Phase 2 正式 dynamic seam compare matrix 固定为 4 个代表性 pair × 4 个 preset，主矩阵不混入 smoothing | 当前需要一个可复跑、可直接写进 final report 的正式 Phase 2 收尾入口；而 smoothing 当前只是补充性结论，混入主矩阵会削弱主结论 | 正式 compare 入口固定为 `scripts/run_phase2_dynamic_compare_suite.py`；代表性 pairs 固定为 `square / mcd1 / traffic2 / walking`；正式 presets 固定为 `baseline_fixed / keyframe_seam10 / trigger_fused_d18_fg008 / adaptive_trigger_fused_d18_fg008` | 继续手工拼命令；或把 smoothing 一并纳入主矩阵，均未采用 | `scripts/run_phase2_dynamic_compare_suite.py`, `scripts/build_phase2_visual_summary.py`, `05_evaluation`, `08_project_status_and_master_plan`, `09_dynamic_seam_and_temporal_eval` |
| DEC-20260323-06 | 2026-03-23 | Phase 2 以“Dynamic seam MVP + meaningful temporal evaluation + 正式 compare matrix + representative visuals”作为完成边界，advanced seam backend 与 smoothing-specific metric 明确延期 | 用户已明确 seam backend 可不做；当前 MVP 路线已经形成正式 suite、代表性可视化、默认 preset 和可解释的 diagnostics，继续拖长只会把 Phase 2 与 advanced 研究混在一起 | Phase 2 可视为完成，后续直接进入 Phase 3；仍未解决的问题只保留为非阻塞 open/deferred issues | 继续把 advanced seam backend、smoothing-specific metric 和 adaptive stable preset 调参全部塞进 Phase 2，未采用 | `08_project_status_and_master_plan`, `09_dynamic_seam_and_temporal_eval`, `14_open_issues_and_next_steps` |
| DEC-20260323-07 | 2026-03-23 | Phase 3 的第一个正式实验块固定为 KITTI `image_02_image_03` color stereo full-length suite，并显式覆盖 `fps=10`、`max_frames=6000`；Phase 3 正式 dynamic seam 子矩阵当前只保留 `baseline_fixed / keyframe_seam10 / trigger_fused_d18_fg008` | 用户明确要求 Phase 3 优先多用 KITTI color stereo pair 且尽量全帧跑完；同时 Phase 2 已证明 `adaptive_trigger_fused` 更适合作为实验 preset，不宜直接塞进 Phase 3 的正式默认矩阵 | 当前正式 Phase 3 入口固定为 `scripts/run_phase3_kitti_compare_suite.py`；正式汇总落在 `outputs/phase3/phase3_kitti_full_v1`；后续 final report 可直接引用其方法表、dynamic seam 表和 pair coverage 表 | 继续手工分别调用 Phase 1 / Phase 2 suite 并手工拼表；或把 `adaptive_trigger_fused` 继续放入 Phase 3 正式默认矩阵，均未采用 | `scripts/run_phase3_kitti_compare_suite.py`, `scripts/build_phase3_kitti_summary.py`, `05_evaluation`, `07_experiments_and_figures`, `08_project_status_and_master_plan` |
| DEC-20260323-08 | 2026-03-23 | Phase 3 正式总表以三个 full-length 数据域 suite 组成：KITTI、DynamicStereo、`mine_source`；当前总表结论以“跨数据域实验事实”为准，而不是预设 Method B 必然优于 Method A | KITTI、DynamicStereo、`mine_source` 的 full-length 结果都已跑通，且三者都显示 `trigger_fused_d18_fg008` 稳定优于 `baseline_fixed / keyframe_seam10`，但当前 Method B 在 fixed-geometry compare 上未整体优于 Method A | Phase 3 正式总表固定为 `outputs/phase3/phase3_overall_full_v1`；final report 应强调跨数据域结论与局限，而不是单数据域叙事 | 继续只保留单一 KITTI 总表；或把 Phase 3 结论只建立在 DynamicStereo / mine_source 上，均未采用 | `scripts/build_phase3_overall_summary.py`, `outputs/phase3/phase3_overall_full_v1`, `05_evaluation`, `08_project_status_and_master_plan`, `14_open_issues_and_next_steps` |
| DEC-20260323-09 | 2026-03-23 | Method B 的正式方法对比应固定使用显式 accuracy preset，而不能继续依赖旧 implicit preset | 本次复盘确认：旧 compare 中 `SuperPoint` preprocess resize 控制存在语义偏差，且 `LightGlue` 使用的是更偏速度的默认 adaptive 配置；这会系统性低估 Method B | 后续 Method B 正式 compare 默认采用：`max_keypoints=4096`、`resize_long_edge=1536`、`depth_confidence=-1`、`width_confidence=-1`、`filter_threshold=0.1`；旧 Phase 3 方法表改为“旧 preset 下的实验事实” | 继续沿用旧 implicit preset 并把 Phase 3 当前方法表直接写进 final report，未采用 | `src/stitching/features.py`, `src/stitching/matching.py`, `06_method2_strong_matching`, `05_evaluation`, `08_project_status_and_master_plan` |
| DEC-20260324-01 | 2026-03-24 | Phase 3 的正式 `Method A vs Method B` compare 以刷新后的 `phase3_*_methods_acc_v2` 和 `phase3_overall_methods_acc_v2` 为准；旧 `*_full_v1` 中的方法表降级为“旧 implicit preset 下的历史结果”，dynamic seam 正式表仍沿用 `*_full_v1` | 2026-03-24 已用显式 Method B accuracy preset 全量重跑 KITTI、DynamicStereo、`mine_source` 的 full-length 方法 compare。刷新后，Method B 在三数据域与 overall 上的 `mean_inliers` 明显上升，不再支持“Method B 整体偏弱”的旧表述 | final report 的方法主表、方法图和结论必须引用 `phase3_*_methods_acc_v2` / `phase3_overall_methods_acc_v2`；dynamic seam 仍引用 `phase3_*_full_v1` / `phase3_overall_full_v1` | 继续沿用旧 `phase3_overall_full_v1` 方法表并只在正文口头修正，未采用 | `scripts/run_video_compare_suite.py`, `scripts/run_phase3_kitti_compare_suite.py`, `outputs/phase3/phase3_*_methods_acc_v2`, `outputs/phase3/phase3_overall_methods_acc_v2`, `05_evaluation`, `06_method2_strong_matching`, `07_experiments_and_figures`, `08_project_status_and_master_plan` |
| DEC-20260324-02 | 2026-03-24 | 当前 `method_b_accuracy_v1` 正式 preset 先冻结为基准线；后续任何 Method B 优化必须以新命名 preset 的方式并存验证，不能直接覆盖正式结果 | 用户明确要求继续优化 Method B，但不能因为追求 `inlier_ratio / fps` 而把当前已经验证过的高内点数结果回退 | 后续若继续优化，应新增 `balanced / no_upsample / tighter_filter / adaptive_lg` 等候选 preset，并与 `method_b_accuracy_v1` 做并行 compare；在 full-length 复验通过前，不修改正式默认 compare | 直接把当前 accuracy preset 改成更快或更高 ratio 的配置，未采用 | `06_method2_strong_matching`, `05_evaluation`, `08_project_status_and_master_plan`, `14_open_issues_and_next_steps` |
| DEC-20260324-03 | 2026-03-24 | `fixed_geometry` 下的正式方法 compare 不再只用 `mean_inliers / mean_inlier_ratio / approx_fps` 三项解释；应扩展为“初始化代价 + 几何质量 + blending artefact + temporal coherence”四类指标 | 当前 Phase 3 已证明 Method B 的 trade-off 不能只靠三项指标解释；同时 Video Blending Quality 论文明确建议同时看 illuminance consistency、artefact（ghosting/bleeding）与 temporal coherence | 后续评测协议应补 `init_ms / steady_state_ms / reprojection_error / overlap_diff / stitched_delta / seam-band consistency / temporal coherence` 等指标；plot/export 也应按这四类组织 | 继续只靠 `inliers / ratio / fps` 三项做 final report 主表，未采用 | `05_evaluation`, `06_method2_strong_matching`, `08_project_status_and_master_plan`, `background/TIP-2020-A Metric for Video Blending Quality Assessment.pdf` |
| DEC-20260324-04 | 2026-03-24 | 本轮 fixed-geometry 的 temporal artefact MVP 指标采用 `seam-band flicker`，不在本轮实现 `flow-compensated temporal residual` | 当前工程已经有 seam band、stitched frame 和 mask-aware absdiff，能低成本落地 `seam-band flicker`；若继续引入 optical flow，会明显扩大评测层复杂度 | 当前正式 bundle 已能导出 `mean_seam_band_flicker`；若后续要进一步加强 temporal metric，再单独立项补 optical flow residual | 本轮直接实现 flow-compensated residual，未采用 | `src/stitching/temporal.py`, `src/stitching/video_stitcher.py`, `scripts/run_baseline_video.py`, `05_evaluation` |
| DEC-20260324-05 | 2026-03-24 | `kp3072_v1` 被记录为当前最值得继续 full-length 验证的 Method B candidate，但在完成多数据域复验前不升格为正式 baseline | representative sweep 显示它比 `accuracy_v1` 更快，overall `reprojection_error` 更低，且在 KITTI / DynamicStereo 上表现接近或更好；但它在 `mine_source_walking_left_right` 上有明显回退 | 后续若继续做 Method B 优化，优先重跑 `kp3072_v1` 的 full-length compare；当前正式 compare 仍使用 `accuracy_v1` | 直接把 `kp3072_v1` 升格为正式默认；或继续盲目追加更多 candidate，不先做 full-length 复验，均未采用 | `src/stitching/method_b_presets.py`, `scripts/run_method_b_preset_sweep.py`, `06_method2_strong_matching`, `08_project_status_and_master_plan` |
| DEC-20260324-06 | 2026-03-24 | 保持 `method_b_accuracy_v1` 为正式默认 preset，并以 `phase3_*_methods_rich_v3` / `phase3_overall_methods_rich_v3` 作为当前 final report 的正式方法主表；stage runtime breakdown 仅解释为 geometry-update event 耗时 | 用户要求默认不变，同时把 richer metrics 纳入全量重跑并进入 final report 图表；重跑后 rich-v3 是当前最完整、可追溯的正式方法结果 | 后续方法 compare、overall 总表和 report 图表一律引用 `*_methods_rich_v3`；`avg_feature_runtime_ms_* / avg_matching_runtime_ms / avg_geometry_runtime_ms` 不能被写成 steady-state 逐帧耗时 | 继续沿用 `*_methods_acc_v2`；或先把 `kp3072_v1` 升格后再重跑，均未采用 | `scripts/run_phase3_full_methods_suite.py`, `scripts/build_phase3_report_figures.py`, `05_evaluation`, `07_experiments_and_figures`, `08_project_status_and_master_plan` |
| DEC-20260324-07 | 2026-03-24 | 清理当前脚本框架：顶层 `scripts/` 只保留正式入口与仍有现实调试价值的辅助工具；旧 phase ablation 与 Method B exploratory sweep 移入 `scripts/legacy/`，Method B active preset 收敛为 `accuracy_v1` 与 `kp3072_v1` | 当前顶层脚本和 preset 面过宽，容易继续误用历史探索脚本与已放弃的候选 preset；用户明确要求先做“标注并移出现有框架” | README、ai-docs 和环境文档的推荐入口被收敛；后续默认工作流只围绕正式入口、auxiliary helper 与 `scripts/legacy/` 三层组织 | 只改文档不移动脚本；继续保留 `no_upsample_v1` / `filter015_v1`，均未采用 | `README.md`, `docs/environment.md`, `scripts/legacy/*`, `src/stitching/method_b_presets.py`, `04_quality_improvement`, `05_evaluation`, `06_method2_strong_matching`, `08_project_status_and_master_plan` |
| DEC-20260324-08 | 2026-03-24 | `kp3072_v1` full-length 多数据域复验完成后，仍不升格为正式 Method B baseline；正式默认继续保持 `accuracy_v1`，`kp3072_v1` 仅作为优化候选对照结果保留 | 虽然 `kp3072_v1` 在 overall 上略微提升了 `mean_inlier_ratio / fps / reprojection_error`，但它把 `mean_inliers` 从约 `748.88` 降到约 `609.58`，且在 `mine_source` 上从约 `842.59` 明显回退到约 `628.65`，不满足“不能负优化”要求 | 正式方法主表继续引用 `phase3_overall_methods_rich_v3`；另保留 `outputs/phase3/phase3_methodb_accuracy_vs_kp3072_v1/` 作为候选复验与方法讨论材料 | 直接把 `kp3072_v1` 升格为正式默认；或仅基于 KITTI / DynamicStereo 局部结果做结论，均未采用 | `outputs/phase3/phase3_methodb_accuracy_vs_kp3072_v1/*`, `05_evaluation`, `06_method2_strong_matching`, `07_experiments_and_figures`, `08_project_status_and_master_plan`, `14_open_issues_and_next_steps` |
| DEC-20260324-09 | 2026-03-24 | Phase 4 GUI 采用标准库 `tkinter` 桌面 thin wrapper：复用 `load_pairs`、`split_sbs_stereo` 的 manifest helper 与 `run_baseline_video.py` 子进程启动，不新增 Web 层或新 pipeline | 用户明确要求“不用 webui”；当前 repo 已有稳定 CLI、manifest 读写辅助和 outputs bundle，最小可用 GUI 应只包这些现有事实来源 | 当前 GUI 入口固定为 `scripts/run_stitching_gui.py`；新 pair 注册统一复制到 `data/raw/Videos/gui_uploads/<pair_id>/` 并写 repo-relative manifest 路径；输出限制在 `outputs/runs/<run_id>/` | 做 Web UI；在 GUI 中重写 stitching 逻辑；允许直接写 repo 外绝对路径，均未采用 | `scripts/run_stitching_gui.py`, `src/stitching/gui_thin_wrapper.py`, `scripts/preprocess/split_sbs_stereo.py`, `src/stitching/io.py`, `README.md`, `docs/environment.md`, `08_project_status_and_master_plan` |
| DEC-20260324-10 | 2026-03-24 | Phase 4 GUI 的后续 polish 继续保持 thin wrapper：existing pair 预览复用 `io.open_pair()`，注册流程改为独立弹窗，run 完成后只提供文件管理器打开入口，不引入新的数据流或批量实验逻辑 | 用户要求提升 GUI 可用性，但当前阶段不能回头把 GUI 做成新的 orchestrator；复用现有 I/O 层和系统文件管理器调用是最小且可维护的路径 | GUI 新增 pair 首帧预览、弹窗式注册和 run 目录打开能力，但仍只服务单 run 流程；不扩展到 frame-directory 注册、compare suite 或 figure export | 继续在主界面堆叠更多注册表单；为 GUI 单独实现视频读取逻辑；把 compare suite 搬进 GUI，均未采用 | `src/stitching/gui_thin_wrapper.py`, `src/stitching/io.py`, `scripts/preprocess/split_sbs_stereo.py`, `08_project_status_and_master_plan`, `14_open_issues_and_next_steps` |
| DEC-20260324-11 | 2026-03-24 | Phase 4 GUI 的 keyframe 参数只在对应模式下显示；首帧预览编码保持 OpenCV 的 BGR 语义，不额外转 RGB | 用户明确要求 keyframe 参数不要在无关模式下占位；同时现有预览已暴露明显蓝偏，根因是对 `cv2.imencode` 输入做了错误的 BGR->RGB 转换 | GUI 当前会根据 `geometry_mode=keyframe_update` 与 `seam_policy=keyframe` 条件显示相关参数；preview 颜色与原图一致，不再蓝偏 | 继续始终显示 keyframe 参数；或只禁用不隐藏；继续保留错误颜色通道，均未采用 | `src/stitching/gui_thin_wrapper.py`, `README.md`, `docs/environment.md`, `08_project_status_and_master_plan` |
| DEC-20260324-12 | 2026-03-24 | Phase 4 GUI 的 `Run Config` 行布局固定为“稳定可见参数在前、条件显示的 keyframe 参数在最后一行” | 用户明确指出：虽然 keyframe 字段已能隐藏，但原位置会在中间留下视觉空块；把 trigger/snapshot/force CPU 前置、keyframe 字段后置是最直接且稳定的布局收敛 | 当前 GUI 的第三行固定展示 `trigger diff / fg ratio / snapshot every / force cpu`；最后一行才展示 `keyframe_every / seam_keyframe_every`，因此隐藏时不会在中间留下空洞 | 继续保留原顺序并尝试动态补位；或引入更复杂的响应式布局系统，均未采用 | `src/stitching/gui_thin_wrapper.py`, `12_implementation_log`, `13_change_log`, `14_open_issues_and_next_steps` |
| DEC-20260324-13 | 2026-03-24 | Phase 4 GUI 的“Register New Pair”流程必须要求用户显式填写唯一 `pair_id`；不再为注册流程自动生成默认 id | 用户明确要求注册时若未填写 `pair_id` 必须拒绝，且检测到重复名称时必须阻止注册；沿用自动 fallback 会制造隐式命名和 manifest 污染 | GUI 现在会拒绝空 `pair_id`、清洗后为空的无效名字以及与现有 pair 重名的情况；run id 的自动 fallback 行为不受影响 | 继续对注册流程使用 `gui_upload_pair` 自动 fallback；或只做重复提示但允许空值，均未采用 | `src/stitching/gui_thin_wrapper.py`, `12_implementation_log`, `13_change_log`, `14_open_issues_and_next_steps` |
| DEC-20260324-14 | 2026-03-24 | `pairs.yaml` 的追加写入默认采用文本级局部插入，不再优先走 ruamel 全文件 round-trip dump | 用户反馈 GUI 注册一个新 pair 时会导致整个 manifest 看起来都被修改；当前根因是 `append_manifest_entries()` 优先使用 ruamel 重新序列化整份 YAML | 后续通过 GUI 或 `split_sbs_stereo` 追加 manifest entry 时，diff 应尽量只包含新增 block；插入位置按 entry 的 `dataset` 就近插入，若无同 dataset 则追加到末尾 | 继续优先使用 ruamel round-trip；或仅在 GUI 侧单独写一个专用 append helper，均未采用 | `scripts/preprocess/split_sbs_stereo.py`, `src/stitching/gui_thin_wrapper.py`, `12_implementation_log`, `13_change_log`, `14_open_issues_and_next_steps` |
| DEC-20260324-15 | 2026-03-24 | Phase 4 GUI 的 `trigger` 参数只在 `seam_policy=trigger` 时显示，并与 keyframe 参数共用一个动态参数行，按 `geometry keyframe -> seam keyframe -> trigger` 的顺序重排 | 用户明确要求 `trigger diff` 等参数不要在非 trigger 模式下占位，且其位置应放在 keyframe 参数之后；继续在固定 grid 里做单格隐藏仍会留下视觉空块 | GUI 现在只有 `Snapshot Every / Force CPU` 保持稳定显示；`Keyframe Every / Seam Keyframe Every / Trigger Diff / FG Ratio` 会按当前模式动态重建并从左到右连续排列 | 继续在固定 grid 上隐藏单个 trigger 字段；或始终显示 trigger 参数但只在命令层忽略，均未采用 | `src/stitching/gui_thin_wrapper.py`, `12_implementation_log`, `13_change_log`, `14_open_issues_and_next_steps` |
| DEC-20260324-16 | 2026-03-24 | Phase 4 GUI 的动态参数区必须与主 `Run Config` 使用同一套 `grid` 列对齐，不再混用外层 `pack` + 错父容器 widget | 用户实机截图表明上一版动态参数区仍严重错位；根因是 label 在子 frame 里，而 entry/check widget 被创建在外层 frame 上，导致布局对象不一致 | 动态参数区现在改为直接在 `dynamic_options_frame` 上使用 `_grid_labeled_widget()` 逐列重建，字段会和主表单保持同风格对齐 | 继续使用横向 `pack` 的子 frame 封装；或只调整 padx/pady 碰运气，均未采用 | `src/stitching/gui_thin_wrapper.py`, `12_implementation_log`, `13_change_log`, `14_open_issues_and_next_steps` |
| DEC-20260325-01 | 2026-03-25 | 正式脚本入口收敛为 `formal / internal / legacy` 三层，并将仍带临时阶段语义的正式脚本重命名为稳定 final 名称 | 当前项目框架已稳定，继续保留 `phase2 / phase3` 风格的顶层脚本名会让正式入口和历史实验脚本混在一起，增加维护成本和使用误判 | 顶层 `scripts/` 只保留最终正式入口与必要辅助工具；内部汇总脚本集中到 `scripts/internal/`；历史实验和 debugging helper 集中到 `scripts/legacy/`；README、环境文档和 ai-docs 同步以新命名为准 | 只改文档不改脚本名；继续保留旧 phase 风格顶层脚本，均未采用 | `scripts/eval_method_compare.py`, `scripts/eval_method_compare_matrix.py`, `scripts/eval_dynamic_compare.py`, `scripts/export_dynamic_visuals.py`, `scripts/export_report_figures.py`, `scripts/internal/*`, `scripts/legacy/*`, `README.md`, `docs/environment.md`, `08_project_status_and_master_plan`, `14_open_issues_and_next_steps` |
| DEC-20260325-02 | 2026-03-25 | 正式环境收敛为单一 `.venv + requirements.txt`；`requirements-methodb.txt` 与 `.venv-methodb` 降级为兼容保留项 | Method B 已经并入正式框架，继续把它作为独立正式环境会增加文档复杂度和使用分叉；当前 `requirements-methodb.txt` 本质只是 `requirements.txt` 的增量包装 | README、`docs/environment.md` 与 current truth 文档统一推荐 `.venv`；`requirements.txt` 变成 Method A + Method B + GUI + compare/export 的完整依赖集；`.venv-methodb` 只在已有本地环境不想重建时作为兼容路径保留 | 继续保留双正式环境；或直接删除 `requirements-methodb.txt`，均未采用 | `requirements.txt`, `requirements-methodb.txt`, `docs/environment.md`, `README.md`, `06_method2_strong_matching`, `08_project_status_and_master_plan`, `14_open_issues_and_next_steps` |

## 变更文件清单
| 文件 | 变更说明 | 负责人 | 状态 |
| --- | --- | --- | --- |
| ai-docs/current/11_decision_log/11_decision_log.md | 新增 append-only 决策日志并写入首批全局决策 | Codex | 完成 |
